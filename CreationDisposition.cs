namespace IO
{
    public enum CreationDisposition: uint
    {
        CreateNew = 1,
        CreateAlways = 2,
        OpenExisting = 3,
        OpenAlways = 4,
        Rewrite = 5
    }
}
